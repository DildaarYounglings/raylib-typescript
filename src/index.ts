import * as raylib from "raylib";
const rl = raylib;

const main = function(){
    const darkGreen = rl.GetColor(0x0ff00);
    const screenWidth = 800;
    const screenHeigt = 600;
    rl.InitWindow(screenWidth,screenHeigt,"raylib Tetris");
    while(!rl.WindowShouldClose()){

    }
}